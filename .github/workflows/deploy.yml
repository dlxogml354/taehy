# GitHub Pages deployment disabled — site is now deployed via Vercel.
# Vercel auto-deploys on every push to main branch.
name: Disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment handled by Vercel"
